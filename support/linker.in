#! /bin/sh

# Wrapper for libtool used as linker
# @configure_input@

NATIVE_ADA="@ADA@"
CROSS_ADA="@ADA_FOR_TARGET@"

case "`basename "$0"`" in
  native-*)
    ADA="${NATIVE_ADA}"
    ;;
  *)
    ADA="${CROSS_ADA}"
    ;;
esac

`dirname $0`/../libtool @LIBTOOL_TAG@ --mode=link $ADA "$@"

