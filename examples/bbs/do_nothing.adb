with Ada.Text_IO; use Ada.Text_IO;

procedure Do_Nothing is

   --  This procedure does nothing, as the server has no main subprogram

begin
   Put_Line ("Server partition starting");
end Do_Nothing;
