with "common", "src", "src_corba";

project tools_po_dumpir is

   Dir := "../tools/po_dumpir";
   for Source_Dirs use (Dir);
   for Object_Dir use Dir;

   package Compiler is

      for Default_Switches ("Ada")
         use Common.Compiler.Common_Ada_Compiler_Switches;

   end Compiler;

   for Main use ("po_dumpir");

end tools_po_dumpir;
