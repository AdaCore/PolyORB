with "common", "src";
limited with "src_corba", "cos_naming";

project idls_cos_naming is

   Dir := "../idls/cos/naming";
   for Source_Dirs use (Dir);
   for Object_Dir use Dir;
   
   for Library_Name use "polyorb-corba-cos-naming";
   for Library_Dir use Dir & "/" & ".libs";
   for Library_Kind use "static";

   package Compiler is

      for Default_Switches ("Ada")
         use Common.Compiler.Common_Ada_Compiler_Switches;

   end Compiler;

end idls_cos_naming;
