configuration Measure is

   pragma starter (ada);

   part1 : partition;
   procedure Local is in part1;

   part2 : partition := (remote);

   function localhost (s : string) return string;
   pragma import (shell, localhost, "hostname ; true");

   for part2'host use localhost;

end Measure;
