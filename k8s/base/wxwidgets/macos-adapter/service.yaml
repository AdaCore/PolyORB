apiVersion: v1
kind: Service
metadata:
  name: macos-adapter
  namespace: wxwidgets
  labels:
    app: macos-adapter
    version: v1.0.0
    tier: platform-adapter
spec:
  type: ClusterIP
  selector:
    app: macos-adapter
    version: v1.0.0
  ports:
  - name: grpc
    port: 50055
    targetPort: 50055
    protocol: TCP
