apiVersion: v1
kind: ConfigMap
metadata:
  name: event-manager-config
  namespace: wxwidgets
  labels:
    app: event-manager
    version: v1.0.0
data:
  log_level: "info"
  grpc_port: "50053"
  event_buffer_size: "2048"
  event_batch_size: "100"
