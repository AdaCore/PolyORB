apiVersion: v1
kind: Secret
metadata:
  name: widget-core-secret
  namespace: wxwidgets
  labels:
    app: widget-core
    version: v1.0.0
type: Opaque
stringData:
  # Replace with actual values in production
  api_key: "REPLACE_WITH_ACTUAL_API_KEY"
  tls_cert: "REPLACE_WITH_TLS_CERT"
  tls_key: "REPLACE_WITH_TLS_KEY"
