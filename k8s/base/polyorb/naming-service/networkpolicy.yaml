apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: naming-service-netpol
  namespace: polyorb
  labels:
    app: naming-service
    version: v1.0.0
spec:
  podSelector:
    matchLabels:
      app: naming-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: polyorb
    ports:
    - protocol: TCP
      port: 50063
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: polyorb
  - to:
    - namespaceSelector:
        matchLabels:
          name: kube-system
    ports:
    - protocol: UDP
      port: 53
