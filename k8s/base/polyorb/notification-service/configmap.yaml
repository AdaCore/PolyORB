apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: polyorb
  labels:
    app: notification-service
    version: v1.0.0
data:
  log_level: "info"
  grpc_port: "50065"
  notification_buffer_size: "2000"
  max_subscriptions: "500"
