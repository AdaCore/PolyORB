configuration Dynamic is
   Partition1 : Partition := (Prime, Controller);
   Partition2 : Partition := (Prime);
   Partition3 : Partition := (Prime);
   procedure Main is in Partition1;
end Dynamic;
