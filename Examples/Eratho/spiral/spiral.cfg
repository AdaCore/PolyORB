configuration Spiral is
   pragma Starter (Ada);
   Partition1 : Partition := (Prime_1);
   Partition2 : Partition := (Prime_2);
   Partition3 : Partition := (Prime_3);
   procedure Master is in Partition1;
end Spiral;
