configuration PingPong is

   function Localhost (P: String) return String;
   pragma Import (Shell, Localhost, "localhost");

   for Partition'Storage_Dir use "bin";
   for Partition'Host use Localhost;

   p1, p2 : Partition;
   procedure Main is in p1;
begin
   p2 := (rci);
end PingPong;
